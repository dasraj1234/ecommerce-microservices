spring:
  application:
    name: authN_Z
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ecommerce_db?serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true&useSSL=false
    username: myuser
    password: mypassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Kolkata